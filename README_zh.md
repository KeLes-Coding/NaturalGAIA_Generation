# NaturalGAIA 生成流程

[English](README.md) | **中文文档**

本仓库包含 **NaturalGAIA** 的生成流程，这是一个基于 Wikidata 知识图谱构建的智能体基准测试。该流程自动化了子图爬取、逻辑任务骨架生成以及使用 LLM 将其细化为自然语言查询的过程。

## 📂 项目结构

```text
.
├── config/              # 配置文件（工具和 LLM 设置）
├── src/                 # 源代码
│   ├── builder.py       # 知识图谱爬虫和构建器
│   ├── generator.py     # 逻辑任务骨架生成器
│   ├── llm_client.py    # LLM API 客户端（支持多提供商）
│   └── utils.py         # 工具函数（日志记录器等）
├── visualization/       # 交互式可视化界面
│   └── app.py           # Streamlit 应用，用于任务与图谱探索
├── data/                # 生成的数据集（图谱和任务）
├── logs/                # 运行时日志
└── main.py              # 程序入口
```

## 🚀 快速开始

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 配置
复制示例配置文件：

```bash
cp config/llm_config.example.yaml config/llm_config.yaml
```
编辑 `config/llm_config.yaml` 添加你的 API Keys（OpenAI/DeepSeek/等）和代理设置。

### 3. 使用方法
运行完整流程：

```bash
# 基本运行（以 Taylor Swift 为种子）
python main.py

# 自定义运行（不同的种子实体和规模）
python main.py --seed 42 --seed_entity Q2 --seed_label Earth --nodes 500 --tasks 50
```

### 4. 交互式可视化

启动基于 Streamlit 的图形界面，探索生成的任务和知识图谱：

```bash
streamlit run visualization/app.py
```

**功能特性：**
- **任务分析面板**
  - 浏览和搜索已生成的任务
  - 查看细化的自然语言查询和标准答案
  - 通过交互式时间线检查多跳推理链
  - 按复杂度和路径长度筛选任务

- **图谱探索面板**
  - 交互式知识图谱可视化（力导向布局）
  - 基于节点度数的着色方案（红色=枢纽节点，黄色=桥接节点，蓝色=叶节点）
  - 点击节点查看详情和连接关系
  - 可调整节点数量以优化性能
  - 基于物理引擎的稳定性优化，确保交互流畅

### 📦 依赖管理

如果你添加了新的库，请更新 `requirements.txt` 以保持环境的可复现性。

**选项 1：使用 pipreqs（推荐，获得更干净的依赖）**
这会根据项目中实际的导入来生成 requirements，避免本地环境的干扰。

```bash
# 如果还没有安装 pipreqs
pip install pipreqs

# 生成 requirements.txt（强制覆盖）
pipreqs . --force --ignore venv,data,logs,visualization,wikidata --encoding=utf-8
```

**选项 2：使用 pip freeze（标准方式）**

```bash
pip freeze > requirements.txt
```

## 🛠️ 技术栈

- **数据来源：** Wikidata（通过 SPARQL）
- **图处理：** NetworkX
- **LLM 集成：** OpenAI SDK / HTTPX
- **并发处理：** ThreadPoolExecutor
- **可视化：** Streamlit + Streamlit-Agraph

## 📄 许可证
MIT License
